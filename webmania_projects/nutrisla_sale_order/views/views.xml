<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="nutrisla.list">
      <field name="name">nutrisla list</field>
      <field name="model">nutrisla.nutrisla</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="nutrisla.action_window">
      <field name="name">nutrisla window</field>
      <field name="res_model">nutrisla.nutrisla</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="nutrisla.action_server">
      <field name="name">nutrisla server</field>
      <field name="model_id" ref="model_nutrisla_nutrisla"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="nutrisla" id="nutrisla.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="nutrisla.menu_1" parent="nutrisla.menu_root"/>
    <menuitem name="Menu 2" id="nutrisla.menu_2" parent="nutrisla.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="nutrisla.menu_1_list" parent="nutrisla.menu_1"
              action="nutrisla.action_window"/>
    <menuitem name="Server to list" id="nutrisla" parent="nutrisla.menu_2"
              action="nutrisla.action_server"/>
    -->
  </data>
</odoo>